class Router {
    constructor() { this.currentPage = 1; this.currentTag = null; this.currentPost = null; this.currentPageSlug = null; this.currentSearch = null; this.currentFavorites = null; this.currentFavPage = 1; this.searchEngine = new SearchEngine(); this.activeScripts = []; this.activeStyles = []; this.init(); }
    init() { this.parseURL(); this.handleRoute(); window.addEventListener('popstate', () => { this.parseURL(); this.handleRoute(); }); document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && document.body.classList.contains('reader-mode')) this.exitReaderMode(); }); }
    parseURL() { const p = new URLSearchParams(window.location.search); this.currentPage = parseInt(p.get('page')) || 1; this.currentTag = p.get('tag') || null; this.currentPost = p.get('post') || null; this.currentPageSlug = p.get('page_slug') || null; this.currentSearch = p.get('search') || null; this.currentFavorites = p.get('favorites') || null; this.currentFavPage = parseInt(p.get('favpage')) || 1; }
    navigate(qs) { const nu = qs ? `index.html${qs}` : 'index.html'; window.history.pushState({}, '', nu); this.parseURL(); this.handleRoute(); }
    async handleRoute() { const mc = document.getElementById('mainContent'); mc.innerHTML = '<div class="loading">در حال بارگذاری...</div>'; this.cleanupPostCode(); BlogEngine.loadExternalScripts(); if (this.currentFavorites === 'all') await this.loadFavoritesPage(); else if (this.currentPost) await this.loadFullPost(this.currentPost); else if (this.currentPageSlug && this.currentPageSlug !== 'home') await this.loadStaticPage(this.currentPageSlug); else if (this.currentSearch) await this.loadSearchResults(this.currentSearch); else if (this.currentTag) await this.loadPostsByTag(this.currentTag); else await this.loadHomepage(); }
    cleanupPostCode() { this.activeScripts.forEach(el => el?.remove()); this.activeScripts = []; this.activeStyles.forEach(el => el?.remove()); this.activeStyles = []; }
    extractCodeFromContent(htmlContent, postMeta) { const scripts = [], styles = []; let cleanHtml = htmlContent; const scriptRegex = /<script\b([^>]*)>([\s\S]*?)<\/script>/gi; let sm; while ((sm = scriptRegex.exec(htmlContent)) !== null) { const srcM = sm[1].match(/src=["']([^"']+)["']/i); scripts.push({ type: srcM ? 'external' : 'inline', src: srcM ? srcM[1] : null, content: sm[2].trim() }); cleanHtml = cleanHtml.replace(sm[0], ''); } const styleRegex = /<style\b[^>]*>([\s\S]*?)<\/style>/gi; let stm; while ((stm = styleRegex.exec(htmlContent)) !== null) { styles.push({ type: 'inline', content: stm[1].trim() }); cleanHtml = cleanHtml.replace(stm[0], ''); } if (postMeta?.file) { const bfn = postMeta.file.replace(/\.html$/i, ''); scripts.push({ type: 'external-file', src: this.getExternalFilePath(postMeta, `${bfn}.js`), content: null }); styles.push({ type: 'external-file', src: this.getExternalFilePath(postMeta, `${bfn}.css`) }); } return { cleanHtml, scripts, styles }; }
    getExternalFilePath(postMeta, fileName) { const sd = postMeta._postSubdir || postMeta._batchSubdir || ''; return sd ? `posts/${sd}/${fileName}` : `posts/${fileName}`; }
    executeScripts(scripts, postSlug) { for (const s of scripts) { if (s.type === 'inline' && s.content) { const se = document.createElement('script'); se.textContent = s.content; se.dataset.postSlug = postSlug; document.body.appendChild(se); this.activeScripts.push(se); } else if (s.src) { const se = document.createElement('script'); se.src = s.src; se.dataset.postSlug = postSlug; document.body.appendChild(se); this.activeScripts.push(se); } } }
    applyStyles(styles, postSlug) { for (const s of styles) { if (s.src) { const le = document.createElement('link'); le.rel = 'stylesheet'; le.href = s.src; le.dataset.postSlug = postSlug; document.head.appendChild(le); this.activeStyles.push(le); } else if (s.content) { const se = document.createElement('style'); se.textContent = s.content; se.dataset.postSlug = postSlug; document.head.appendChild(se); this.activeStyles.push(se); } } }

    async loadHomepage() { const mc = document.getElementById('mainContent'); this.updateActiveMenu('home'); try { const pp = await BlogEngine.loadPostsForPage(this.currentPage, 10); if (pp.length === 0 && this.currentPage > 1) { this.navigate(''); return; } if (pp.length === 0 && this.currentPage === 1) { mc.innerHTML = '<p class="no-posts">هیچ مطلبی نیست.</p>'; document.getElementById('pagination').innerHTML = ''; return; } await BlogEngine.resolvePostTitles(pp); await this.renderPostCards(pp); const pag = new Pagination(await BlogEngine.getTotalPostCount(), 10); document.getElementById('pagination').style.display = 'flex'; pag.render(this.currentPage, (pg) => { const pm = new URLSearchParams(); pm.set('page', pg); if (this.currentTag) pm.set('tag', this.currentTag); this.navigate(`?${pm.toString()}`); }); await MenuBuilder.buildSidebarMenu(); await MenuBuilder.buildTagsCloud(); } catch (e) { mc.innerHTML = '<p class="no-posts">خطا در بارگذاری.</p>'; } }
    async loadPostsByTag(tag) { const mc = document.getElementById('mainContent'); mc.innerHTML = `<div class="loading">در حال بارگذاری پست‌های "${tag}"...</div>`; this.updateActiveMenu(null); try { const sp = BlogEngine.sortPosts(BlogEngine.getPostsByTag(await BlogEngine.loadAllPosts(), tag)); if (!sp.length) { mc.innerHTML = '<p class="no-posts">هیچ مطلبی با این برچسب یافت نشد.</p>'; document.getElementById('pagination').innerHTML = ''; return; } const pag = new Pagination(sp.length, 10); const pp = pag.getPageItems(sp, this.currentPage); await BlogEngine.resolvePostTitles(pp); await this.renderPostCards(pp); document.getElementById('pagination').style.display = 'flex'; pag.render(this.currentPage, (pg) => { this.navigate(`?page=${pg}&tag=${encodeURIComponent(tag)}`); }); } catch (e) { mc.innerHTML = '<p class="no-posts">خطا.</p>'; } }

    async loadFavoritesPage() {
        const mc = document.getElementById('mainContent'); mc.innerHTML = '<div class="loading">در حال بارگذاری...</div>'; this.updateActiveMenu(null);
        try {
            const r = await fetch('data/favorite.json'); if (!r.ok) { mc.innerHTML = '<p class="no-posts">خطا.</p>'; return; }
            const favs = await r.json(); if (!favs?.length) { mc.innerHTML = '<p class="no-posts">هیچ مطلب منتخبی یافت نشد.</p>'; document.getElementById('pagination').style.display = 'none'; return; }
            const sorted = favs.sort((a, b) => b.id - a.id);
            const fp = this.currentFavPage || 1, per = 10, totalPages = Math.ceil(sorted.length / per);
            const items = sorted.slice((fp-1)*per, fp*per);
            if (!items.length) { this.navigate('?favorites=all'); return; }
            const posts = items.map(p => ({...p, slug: p.slug || BlogEngine.getSlugFromFile(p.file), _postSubdir: p.subdir || null, _batchSubdir: '', _hasPreviewFile: p.previewfile === 'yes', date: p.date || '', time: p.time || ''}));
            await BlogEngine.resolvePostTitles(posts);
            const results = await Promise.all(posts.map(async (p) => { try { let ex, hm, dt; if (p._hasPreviewFile) { const pc = await BlogEngine.loadPreviewContent(p); if (pc) return { p, ex: pc, hm: true, dt: p._previewTitle || BlogEngine.getDisplayTitle(p) }; } const pd = await BlogEngine.loadPostContentByPost(p); if (!pd) return null; const ext = BlogEngine.extractExcerptAndFull(pd.content); return { p, ex: ext.excerpt, hm: ext.hasMore, dt: BlogEngine.getDisplayTitle(p) }; } catch (e) { return null; } }));
            let html = ''; for (const r of results) { if (r) html += this.buildPostCardHTML(r.p, r.ex, r.hm, r.dt, true); }
            mc.innerHTML = html || '<p class="no-posts">هیچ مطلب منتخبی یافت نشد.</p>';
            if (totalPages > 1) { document.getElementById('pagination').style.display = 'flex'; new Pagination(sorted.length, 10).render(fp, (pg) => this.navigate(`?favorites=all&favpage=${pg}`)); }
            else document.getElementById('pagination').style.display = 'none';
            this.attachPostCardEvents();
        } catch (e) { mc.innerHTML = '<p class="no-posts">خطا.</p>'; }
    }

    buildPostCardHTML(post, ex, hm, dt, isFav = false) {
        const pu = `?post=${encodeURIComponent(post.slug)}`;
        const dth = (() => { const dp = post.date ? DateConverter.toJalali(post.date) : ''; const tp = BlogEngine.formatTime(post.time); if (dp && tp) return `<span class="post-date">${dp} - ${tp}</span>`; if (dp) return `<span class="post-date">${dp}</span>`; if (tp) return `<span class="post-date">${tp}</span>`; return ''; })();
        const badge = isFav ? '<span class="fixed-badge" style="background:#f59e0b;">⭐ منتخب</span>' : (post.fixed === 'yes' ? '<span class="fixed-badge">📌 پست ثابت</span>' : '');
        return `<article class="post-card ${!isFav && post.fixed === 'yes' ? 'fixed-post' : ''}"><div class="post-meta"><div class="post-meta-left">${dth}${badge}</div>${isFav ? '' : this.createPostToolbar(post.slug)}</div><div class="post-title-row"><h2 class="post-title"><a href="${pu}" class="post-title-link" data-post-slug="${post.slug}">${dt}</a></h2></div>${post.image ? `<img src="assets/images/${post.image}" alt="${post.alt || dt}" class="post-image" loading="lazy">` : ''}<div class="post-excerpt" data-post-slug="${post.slug}">${ex}</div>${hm ? `<a href="${pu}" class="read-more" data-post-slug="${post.slug}">ادامه مطلب</a>` : ''}${post.tags?.length ? `<div class="tags">${post.tags.map(t => `<a href="?tag=${encodeURIComponent(t)}" class="tag" data-tag="${t}">${t}</a>`).join('')}</div>` : ''}</article>`;
    }

    createPostToolbar(slug) { return `<div class="post-toolbar" data-post-slug="${slug}"><button class="font-increase" title="افزایش فونت" data-action="font-increase">A⁺</button><button class="font-reset" title="بازنشانی فونت" data-action="font-reset">↺</button><button class="font-decrease" title="کاهش فونت" data-action="font-decrease">A⁻</button><button class="print-pdf" title="پرینت PDF" data-action="print-pdf">🖨️</button><button class="reader-mode-btn" title="نمایش کتابی" data-action="reader-mode">📖</button></div>`; }
    adjustFontSize(slug, action) { const ce = document.querySelector(`.post-content[data-post-slug="${slug}"]`) || document.querySelector(`.post-excerpt[data-post-slug="${slug}"]`); const ft = ce?.closest('.post-card, .full-post')?.querySelector('.post-title, .post-title-link, h1, h2') || document.querySelector(`.post-title-link[data-post-slug="${slug}"]`); if (ce) { const cs = parseFloat(getComputedStyle(ce).fontSize); if (action === 'font-increase') ce.style.fontSize = (cs+1)+'px'; else if (action === 'font-decrease' && cs > 10) ce.style.fontSize = (cs-1)+'px'; else if (action === 'font-reset') ce.style.fontSize = ''; } if (ft) { const ts = parseFloat(getComputedStyle(ft).fontSize); if (action === 'font-increase') ft.style.fontSize = (ts+1)+'px'; else if (action === 'font-decrease' && ts > 12) ft.style.fontSize = (ts-1)+'px'; else if (action === 'font-reset') ft.style.fontSize = ''; } }
    async printPDF(slug) { const ce = document.querySelector(`.post-content[data-post-slug="${slug}"]`) || document.querySelector(`.post-excerpt[data-post-slug="${slug}"]`); if (!ce) return; try { const { jsPDF } = window.jspdf; const doc = new jsPDF('p','mm','a4'); const canvas = await html2canvas(ce, { scale: 2, useCORS: true, allowTaint: true, backgroundColor: '#ffffff' }); const id = canvas.toDataURL('image/jpeg', 0.95); const iw = 190, ph = 277, ih = (canvas.height*iw)/canvas.width; let hl = ih, pos = 10; doc.addImage(id, 'JPEG', 10, pos, iw, ih); while ((hl -= ph) > 0) { pos = hl - ih; doc.addPage(); doc.addImage(id, 'JPEG', 10, pos, iw, ih); } doc.save(`post-${slug}.pdf`); } catch (e) { alert('خطا در تولید PDF.'); } }
    enterReaderMode() { document.body.classList.add('reader-mode'); if (!document.querySelector('.reader-mode-exit')) { const eb = document.createElement('button'); eb.className = 'reader-mode-exit'; eb.innerHTML = '&times;'; eb.title = 'خروج از حالت کتابی'; eb.addEventListener('click', () => this.exitReaderMode()); document.body.appendChild(eb); } }
    exitReaderMode() { document.body.classList.remove('reader-mode'); document.querySelector('.reader-mode-exit')?.remove(); }
    toggleReaderMode() { document.body.classList.contains('reader-mode') ? this.exitReaderMode() : this.enterReaderMode(); }
    attachToolbarEvents() { document.querySelectorAll('.post-toolbar button').forEach(btn => { btn.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); const a = btn.dataset.action, s = btn.closest('.post-toolbar').dataset.postSlug; if (a.startsWith('font-')) this.adjustFontSize(s, a); else if (a === 'print-pdf') this.printPDF(s); else if (a === 'reader-mode') this.toggleReaderMode(); }); }); }

    async renderPostCards(posts) { const mc = document.getElementById('mainContent'); if (!posts?.length) { mc.innerHTML = '<p class="no-posts">هیچ مطلبی یافت نشد.</p>'; return; } const results = await Promise.all(posts.map(async (p) => { try { let ex, hm, dt; if (p._hasPreviewFile) { const pc = await BlogEngine.loadPreviewContent(p); if (pc) return { p, ex: pc, hm: true, dt: p._previewTitle || BlogEngine.getDisplayTitle(p) }; } const pd = await BlogEngine.loadPostContentByPost(p); if (!pd) return null; const ext = BlogEngine.extractExcerptAndFull(pd.content); return { p, ex: ext.excerpt, hm: ext.hasMore, dt: BlogEngine.getDisplayTitle(p) }; } catch (e) { return null; } })); let html = ''; for (const r of results) { if (r) html += this.buildPostCardHTML(r.p, r.ex, r.hm, r.dt); } mc.innerHTML = html; this.attachPostCardEvents(); this.attachToolbarEvents(); setTimeout(() => BlogEngine.rerenderExternalScripts(), 200); }
    attachPostCardEvents() { document.querySelectorAll('.post-title-link').forEach(l => l.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); if (l.dataset.postSlug) { this.navigate(`?post=${encodeURIComponent(l.dataset.postSlug)}`); window.scrollTo({ top: 0, behavior: 'smooth' }); } })); document.querySelectorAll('.read-more').forEach(b => b.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); if (b.dataset.postSlug) { this.navigate(`?post=${encodeURIComponent(b.dataset.postSlug)}`); window.scrollTo({ top: 0, behavior: 'smooth' }); } })); document.querySelectorAll('.tag[data-tag]').forEach(t => t.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); if (t.dataset.tag) this.navigate(`?tag=${encodeURIComponent(t.dataset.tag)}`); })); }
    async loadFullPost(slug) { const mc = document.getElementById('mainContent'); mc.innerHTML = '<div class="loading">در حال بارگذاری...</div>'; this.updateActiveMenu(null); this.cleanupPostCode(); try { const pd = await BlogEngine.loadPostContent(slug); if (!pd) { mc.innerHTML = '<p class="no-posts">یافت نشد.</p>'; return; } const { full } = BlogEngine.extractExcerptAndFull(pd.content); const dt = BlogEngine.getDisplayTitle(pd.meta); const dth = (() => { const dp = pd.meta.date ? DateConverter.toJalali(pd.meta.date) : ''; const tp = BlogEngine.formatTime(pd.meta.time); return (dp && tp) ? `<span class="post-date">${dp} - ${tp}</span>` : dp ? `<span class="post-date">${dp}</span>` : tp ? `<span class="post-date">${tp}</span>` : ''; })(); const { cleanHtml, scripts, styles } = this.extractCodeFromContent(BlogEngine.removeMoreTag(full), pd.meta); let html = `<article class="full-post"><div class="post-meta"><div class="post-meta-left">${dth}${pd.meta.fixed === 'yes' ? '<span class="fixed-badge">📌 پست ثابت</span>' : ''}</div>${this.createPostToolbar(slug)}</div><div class="post-title-row"><h1 class="post-title">${dt}</h1></div>${pd.meta.image ? `<img src="assets/images/${pd.meta.image}" alt="${pd.meta.alt || dt}" class="post-image" loading="lazy">` : ''}<div class="post-content" data-post-slug="${slug}">${cleanHtml}</div>${pd.meta.tags?.length ? `<div class="tags">${pd.meta.tags.map(t => `<a href="?tag=${encodeURIComponent(t)}" class="tag" data-tag="${t}">${t}</a>`).join('')}</div>` : ''}<div class="back-to-home"><a href="index.html" class="back-link">← بازگشت به صفحه اصلی</a></div></article>`; mc.innerHTML = html; document.getElementById('pagination').style.display = 'none'; if (styles.length) this.applyStyles(styles, slug); if (scripts.length) setTimeout(() => this.executeScripts(scripts, slug), 100); this.attachToolbarEvents(); setTimeout(() => BlogEngine.rerenderExternalScripts(), 300); } catch (e) { mc.innerHTML = '<p class="no-posts">خطا.</p>'; } }
    async loadStaticPage(slug) { const mc = document.getElementById('mainContent'); mc.innerHTML = '<div class="loading">در حال بارگذاری...</div>'; this.updateActiveMenu(slug); try { const pd = await BlogEngine.loadPageContent(slug); if (!pd) { mc.innerHTML = '<p class="no-posts">یافت نشد.</p>'; return; } mc.innerHTML = `<article class="full-post"><div class="post-meta"><div class="post-meta-left"></div>${this.createPostToolbar(slug)}</div><div class="post-title-row"><h1 class="post-title">${pd.meta.title}</h1></div><div class="post-content" data-post-slug="${slug}">${pd.content}</div><div class="back-to-home"><a href="index.html" class="back-link">← بازگشت به صفحه اصلی</a></div></article>`; document.getElementById('pagination').style.display = 'none'; this.attachToolbarEvents(); setTimeout(() => BlogEngine.rerenderExternalScripts(), 300); } catch (e) { mc.innerHTML = '<p class="no-posts">خطا.</p>'; } }
    async loadSearchResults(query) { const mc = document.getElementById('mainContent'); mc.innerHTML = '<div class="loading">در حال جستجو...</div>'; this.updateActiveMenu(null); document.getElementById('pagination').style.display = 'none'; try { const { results, total } = await this.searchEngine.performFullSearch(query); if (!results.length) { mc.innerHTML = `<div class="search-results-main"><div class="search-header"><h2>جستجو برای: <span class="search-query">"${this.escapeHtml(query)}"</span></h2></div><p class="no-posts">نتیجه‌ای یافت نشد.</p></div>`; return; } let html = `<div class="search-results-main"><div class="search-header"><h2>نتایج: <span class="search-query">"${this.escapeHtml(query)}"</span></h2><p>${total} نتیجه</p></div>`; results.forEach(r => { html += `<div class="search-result-card"><span class="result-type-badge ${r.type}">${r.type === 'post' ? 'مطلب' : r.type === 'page' ? 'صفحه' : 'برچسب'}</span><h3><a href="${r.url}" class="post-title-link">${this.searchEngine.highlightText(r.title, query)}</a></h3><p>${this.searchEngine.highlightText(r.excerpt, query)}</p></div>`; }); html += '</div>'; mc.innerHTML = html; } catch (e) { mc.innerHTML = '<p class="no-posts">خطا.</p>'; } }
    escapeHtml(t) { const m = {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}; return t.replace(/[&<>"']/g, c => m[c]); }
    updateActiveMenu(slug) { document.querySelectorAll('.horizontal-menu a').forEach(a => a.classList.remove('active')); if (slug === 'home') document.querySelector('[data-page="home"]')?.classList.add('active'); else if (slug) document.querySelector(`[data-page="${slug}"]`)?.classList.add('active'); }
}